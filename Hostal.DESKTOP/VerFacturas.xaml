<UserControl x:Class="Hostal.DESKTOP.VerFacturas"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hostal.DESKTOP"
        mc:Ignorable="d">
    <Grid Height="540" Width="1020" Background="White">
        <DataGrid AutoGenerateColumns="False" x:Name="dta_Facturas" HorizontalAlignment="Left" Background="White" Foreground="#DD000000" Margin="50,150,0,0" BorderBrush="Black" VerticalAlignment="Top" Height="194" Width="400" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="N° Factura"  Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Empresa"  Binding="{Binding NomEmpresa}"/>
                <DataGridTextColumn Header="Fecha de Facturación"  Binding="{Binding FechaFacturacion}"/>
                <DataGridTextColumn Header="Total"  Binding="{Binding Total}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DataGrid x:Name="dta_Detalle" HorizontalAlignment="Left" Background="White" Foreground="#DD000000" Margin="518,150,0,0" BorderBrush="Black" VerticalAlignment="Top" Height="194" Width="415" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Huesped"  Binding="{Binding Nombre}"/>
                <DataGridTextColumn Header="N° Habitación"  Binding="{Binding Nombre}"/>
                <DataGridTextColumn Header="Ingreso"  Binding="{Binding Nombre}"/>
                <DataGridTextColumn Header="Salida"  Binding="{Binding Nombre}"/>
                <DataGridTextColumn Header="Servicio"  Binding="{Binding Cantidad}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
